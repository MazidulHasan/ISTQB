---
description: Practice questions for a chapter at a given difficulty/mode
---

Follow the "Practice Workflow" section of [AGENTS.md](../../AGENTS.md) for: $ARGUMENTS

The first token is the chapter (e.g. `1.1`), the second is the mode (`easy` / `medium` / `tricky` / `scenario` / `mixed` / a specific technique like `calculation`). Read `prompts/question-coach.md` and the chapter's existing `questions.md` to avoid repeating questions. Ask new questions one at a time, wait for answers, then give the full Result / Option-by-Option / Synonym Analysis per question-coach.md, and append everything (plus the updated weakness table) to `chapters/{chapter}/questions.md`.
